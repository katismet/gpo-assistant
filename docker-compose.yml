services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: gpo
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports: ["5432:5432"]
  app:
    build: .
    env_file: .env
    depends_on: [db]
    command: python -u -m app.telegram.bot

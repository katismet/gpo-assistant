<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            color: #333;
        }
        .header {
            text-align: center;
            border-bottom: 2px solid #333;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            color: #2c3e50;
        }
        .header .date {
            margin-top: 10px;
            font-size: 14px;
            color: #7f8c8d;
        }
        .section {
            margin-bottom: 25px;
        }
        .section h2 {
            font-size: 18px;
            color: #34495e;
            border-bottom: 1px solid #bdc3c7;
            padding-bottom: 5px;
            margin-bottom: 15px;
        }
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        .info-item {
            padding: 10px;
            background-color: #f8f9fa;
            border-radius: 5px;
        }
        .info-label {
            font-weight: bold;
            color: #2c3e50;
        }
        .info-value {
            margin-top: 5px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #bdc3c7;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #34495e;
            color: white;
            font-weight: bold;
        }
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        .plan-fact-table th:nth-child(1) { width: 40%; }
        .plan-fact-table th:nth-child(2) { width: 20%; }
        .plan-fact-table th:nth-child(3) { width: 20%; }
        .plan-fact-table th:nth-child(4) { width: 20%; }
        .incidents-table th:nth-child(1) { width: 20%; }
        .incidents-table th:nth-child(2) { width: 80%; }
        .downtime-table th:nth-child(1) { width: 20%; }
        .downtime-table th:nth-child(2) { width: 80%; }
        .efficiency-table th:nth-child(1) { width: 40%; }
        .efficiency-table th:nth-child(2) { width: 30%; }
        .efficiency-table th:nth-child(3) { width: 30%; }
        .signatures {
            margin-top: 40px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
        }
        .signature-block {
            text-align: center;
        }
        .signature-line {
            border-bottom: 1px solid #333;
            height: 40px;
            margin-bottom: 10px;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 12px;
            color: #7f8c8d;
            border-top: 1px solid #bdc3c7;
            padding-top: 20px;
        }
        .notes {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 20px;
        }
        .notes h3 {
            margin-top: 0;
            color: #856404;
        }
        .efficiency-good {
            color: #28a745;
            font-weight: bold;
        }
        .efficiency-warning {
            color: #ffc107;
            font-weight: bold;
        }
        .efficiency-bad {
            color: #dc3545;
            font-weight: bold;
        }
        .incident-item {
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            border-radius: 5px;
            padding: 10px;
            margin-bottom: 10px;
        }
        .downtime-item {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 5px;
            padding: 10px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{ title }}</h1>
        <div class="date">Дата: {{ date }} | Время: {{ time }}</div>
    </div>

    <div class="section">
        <h2>Информация о смене</h2>
        <div class="info-grid">
            <div class="info-item">
                <div class="info-label">Объект:</div>
                <div class="info-value">{{ report_data.object_name or 'Не указан' }}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Тип смены:</div>
                <div class="info-value">{{ report_data.shift_type or 'Не указан' }}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Дата смены:</div>
                <div class="info-value">{{ report_data.shift_date or 'Не указана' }}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Время работы:</div>
                <div class="info-value">{{ report_data.start_time or 'Не указано' }} - {{ report_data.end_time or 'Не указано' }}</div>
            </div>
        </div>
    </div>

    {% if report_data.plan_fact %}
    <div class="section">
        <h2>План-факт</h2>
        <table class="plan-fact-table">
            <thead>
                <tr>
                    <th>Показатель</th>
                    <th>План</th>
                    <th>Факт</th>
                    <th>Отклонение</th>
                </tr>
            </thead>
            <tbody>
                {% for item in report_data.plan_fact %}
                <tr>
                    <td>{{ item.name }}</td>
                    <td>{{ item.plan }}</td>
                    <td>{{ item.fact }}</td>
                    <td>
                        {% if item.deviation > 0 %}
                            <span class="efficiency-good">+{{ item.deviation }}%</span>
                        {% elif item.deviation < 0 %}
                            <span class="efficiency-bad">{{ item.deviation }}%</span>
                        {% else %}
                            {{ item.deviation }}%
                        {% endif %}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}

    {% if report_data.efficiency %}
    <div class="section">
        <h2>Эффективность</h2>
        <table class="efficiency-table">
            <thead>
                <tr>
                    <th>Тип эффективности</th>
                    <th>Значение</th>
                    <th>Оценка</th>
                </tr>
            </thead>
            <tbody>
                {% if report_data.efficiency.raw %}
                <tr>
                    <td>Raw (автоматическая)</td>
                    <td>{{ report_data.efficiency.raw }}%</td>
                    <td>
                        {% if report_data.efficiency.raw >= 90 %}
                            <span class="efficiency-good">Отлично</span>
                        {% elif report_data.efficiency.raw >= 80 %}
                            <span class="efficiency-good">Хорошо</span>
                        {% elif report_data.efficiency.raw >= 70 %}
                            <span class="efficiency-warning">Удовлетворительно</span>
                        {% else %}
                            <span class="efficiency-bad">Неудовлетворительно</span>
                        {% endif %}
                    </td>
                </tr>
                {% endif %}
                {% if report_data.efficiency.user %}
                <tr>
                    <td>User (пользовательская)</td>
                    <td>{{ report_data.efficiency.user }}%</td>
                    <td>
                        {% if report_data.efficiency.user >= 90 %}
                            <span class="efficiency-good">Отлично</span>
                        {% elif report_data.efficiency.user >= 80 %}
                            <span class="efficiency-good">Хорошо</span>
                        {% elif report_data.efficiency.user >= 70 %}
                            <span class="efficiency-warning">Удовлетворительно</span>
                        {% else %}
                            <span class="efficiency-bad">Неудовлетворительно</span>
                        {% endif %}
                    </td>
                </tr>
                {% endif %}
                {% if report_data.efficiency.final %}
                <tr>
                    <td>Final (итоговая)</td>
                    <td>{{ report_data.efficiency.final }}%</td>
                    <td>
                        {% if report_data.efficiency.final >= 90 %}
                            <span class="efficiency-good">Отлично</span>
                        {% elif report_data.efficiency.final >= 80 %}
                            <span class="efficiency-good">Хорошо</span>
                        {% elif report_data.efficiency.final >= 70 %}
                            <span class="efficiency-warning">Удовлетворительно</span>
                        {% else %}
                            <span class="efficiency-bad">Неудовлетворительно</span>
                        {% endif %}
                    </td>
                </tr>
                {% endif %}
            </tbody>
        </table>
    </div>
    {% endif %}

    {% if report_data.incidents %}
    <div class="section">
        <h2>Инциденты</h2>
        {% for incident in report_data.incidents %}
        <div class="incident-item">
            <strong>{{ incident.time }}</strong> - {{ incident.description }}
        </div>
        {% endfor %}
    </div>
    {% endif %}

    {% if report_data.downtime %}
    <div class="section">
        <h2>Простои</h2>
        {% for downtime in report_data.downtime %}
        <div class="downtime-item">
            <strong>{{ downtime.time }}</strong> - {{ downtime.reason }}
        </div>
        {% endfor %}
    </div>
    {% endif %}

    {% if report_data.resources %}
    <div class="section">
        <h2>Использованные ресурсы</h2>
        
        {% if report_data.resources.technique %}
        <h3>Техника</h3>
        <table>
            <thead>
                <tr>
                    <th>Наименование</th>
                    <th>Количество</th>
                    <th>Единица</th>
                    <th>Тариф</th>
                    <th>Стоимость</th>
                </tr>
            </thead>
            <tbody>
                {% for item in report_data.resources.technique %}
                <tr>
                    <td>{{ item.name }}</td>
                    <td>{{ item.qty }}</td>
                    <td>{{ item.unit }}</td>
                    <td>{{ item.tariff or '-' }}</td>
                    <td>{{ item.cost or '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}

        {% if report_data.resources.materials %}
        <h3>Материалы</h3>
        <table>
            <thead>
                <tr>
                    <th>Наименование</th>
                    <th>Количество</th>
                    <th>Единица</th>
                    <th>Цена</th>
                    <th>Стоимость</th>
                </tr>
            </thead>
            <tbody>
                {% for item in report_data.resources.materials %}
                <tr>
                    <td>{{ item.name }}</td>
                    <td>{{ item.qty }}</td>
                    <td>{{ item.unit }}</td>
                    <td>{{ item.price or '-' }}</td>
                    <td>{{ item.cost or '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}

        {% if report_data.resources.timesheet %}
        <h3>Табель</h3>
        <table>
            <thead>
                <tr>
                    <th>Бригада</th>
                    <th>Часы</th>
                    <th>Ставка</th>
                    <th>Стоимость</th>
                    <th>Примечания</th>
                </tr>
            </thead>
            <tbody>
                {% for item in report_data.resources.timesheet %}
                <tr>
                    <td>{{ item.crew }}</td>
                    <td>{{ item.hours }}</td>
                    <td>{{ item.rate }}</td>
                    <td>{{ item.total_cost or '-' }}</td>
                    <td>{{ item.notes or '-' }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}
    </div>
    {% endif %}

    {% if report_data.notes %}
    <div class="section">
        <div class="notes">
            <h3>Примечания</h3>
            <p>{{ report_data.notes }}</p>
        </div>
    </div>
    {% endif %}

    <div class="signatures">
        <div class="signature-block">
            <div class="signature-line"></div>
            <div>Прораб</div>
        </div>
        <div class="signature-block">
            <div class="signature-line"></div>
            <div>Владелец</div>
        </div>
    </div>

    <div class="footer">
        <p>Документ сгенерирован автоматически {{ generated_at }}</p>
        <p>ГПО-Помощник v0.1.0</p>
    </div>
</body>
</html>

# Инструкция по синхронизации новых полей Bitrix24

## ✅ Выполнено

1. **Добавлены новые поля в маппинг** (`app/bitrix_field_map.py`):
   - Смена: `UF_DOWNTIME_REASON`, `UF_SHIFT_PHOTOS`
   - Ресурс: `UF_RES_COMMENT`, `UF_RES_PHOTOS`
   - Табель: `UF_TS_COMMENT`, `UF_TS_PHOTOS`

2. **Команда синхронизации готова**: `make sync-bitrix`

## 📋 Что нужно сделать

### Шаг 1: Создать поля в Bitrix24

Войдите в Bitrix24 и создайте следующие поля в соответствующих смарт-процессах:

#### Смена (entityTypeId: 1050)
- **Причина простоя** (UF_DOWNTIME_REASON)
  - Тип: Многострочный текст
- **Фото смены** (UF_SHIFT_PHOTOS)
  - Тип: Файл (множественный)

#### Ресурс (entityTypeId: 1056)
- **Комментарий к ресурсу** (UF_RES_COMMENT)
  - Тип: Текст
- **Фото ресурса** (UF_RES_PHOTOS)
  - Тип: Файл (множественный)

#### Табель (entityTypeId: 1060)
- **Комментарий к табелю** (UF_TS_COMMENT)
  - Тип: Текст
- **Фото табеля** (UF_TS_PHOTOS)
  - Тип: Файл (множественный)

**Важно**: Используйте точные названия полей, указанные выше (например, "Причина простоя", а не "Причина простоя смены").

### Шаг 2: Запустить синхронизацию

После создания всех полей выполните:

```bash
make sync-bitrix
```

Или напрямую:

```bash
python scripts/sync_bitrix_env.py
```

### Шаг 3: Проверить результат

После синхронизации проверьте файл `bitrix_field_map.json` - в нем должны появиться новые поля с реальными кодами (например, `UF_CRM_7_UF_DOWNTIME_REASON`).

## 🔍 Проверка синхронизации

Проверьте, что новые поля появились в `bitrix_field_map.json`:

```bash
# Windows PowerShell
Get-Content bitrix_field_map.json | Select-String -Pattern "DOWNTIME|PHOTOS|COMMENT"
```

Или откройте файл и проверьте наличие полей в секциях "Смена", "Ресурс" и "Табель".

## 💡 Использование в коде

После синхронизации поля можно использовать:

```python
from app.bitrix_field_map import resolve_code, upper_to_camel

# Получить код поля
f_downtime = resolve_code("Смена", "UF_DOWNTIME_REASON")
f_photos = resolve_code("Смена", "UF_SHIFT_PHOTOS")

# Конвертировать в camelCase для API
f_downtime_camel = upper_to_camel(f_downtime)

# Использовать при обновлении
await bx("crm.item.update", {
    "entityTypeId": 1050,
    "id": shift_id,
    "fields": {
        f_downtime_camel: "Простой из-за погоды",
        f_photos_camel: [file_id1, file_id2]  # Для множественных файлов
    }
})
```




# Как запустить бота

## 🚀 Запуск бота

### Вариант 1: Через bot.py (рекомендуется)

```bash
python app/telegram/bot.py
```

### Вариант 2: Через main.py

```bash
python app/main.py
```

### Вариант 3: Если используется виртуальное окружение

```powershell
# Активировать виртуальное окружение
. venv\Scripts\Activate.ps1

# Запустить бота
python app/telegram/bot.py
```

## ✅ Проверка, что бот запущен

После запуска вы должны увидеть в консоли:
- `Запуск бота...`
- `Bot ID: XXXX`
- `Routers count: X`
- `Начало polling...`

## 📝 Логи

После запуска бота логи будут записываться в:
- `logs/app.log` - все логи
- `logs/errors.log` - только ошибки
- `bot.log` - логи из bot.py

## ⚠️ Если бот не запускается

1. Проверьте, что установлены все зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Проверьте, что в `.env` есть `BOT_TOKEN`:
   ```bash
   # .env должен содержать:
   BOT_TOKEN=ваш_токен_бота
   ```

3. Проверьте ошибки в консоли при запуске

## 🔍 После запуска

После запуска бота:
1. Отправьте `/start` в боте
2. Проверьте логи:
   ```powershell
   Get-Content logs\app.log -Tail 50 | Select-String "\[START\]"
   ```



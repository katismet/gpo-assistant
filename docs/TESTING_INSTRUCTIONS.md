# 🚀 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ БОТА И ИНТЕГРАЦИИ С BITRIX24

## ✅ Статус системы:
- **Бот запущен**: ✅ Активен (@GPO_Helper2_bot)
- **Bitrix24 API**: ✅ Подключен (https://b24-f2m1sn.bitrix24.ru)
- **База данных**: ✅ Инициализирована
- **Вебхук**: ✅ Настроен и протестирован

## 🧪 План тестирования:

### 1. Базовое тестирование бота
1. Найдите бота в Telegram: @GPO_Helper2_bot
2. Отправьте команду `/start`
3. Проверьте, что бот отвечает

### 2. Тестирование создания плана
1. Используйте команду для создания плана
2. Введите данные объекта
3. Введите объемы работ
4. Подтвердите создание

### 3. Проверка интеграции с Bitrix24
После создания плана через бота:
1. Запустите: `python tools/monitor_bitrix.py`
2. Проверьте, что в Bitrix24 появились новые записи:
   - Объект в смарт-процессе "Объект" (ID: 1046)
   - Смена в смарт-процессе "Смена" (ID: 1050)

### 4. Тестирование завершения смены
1. Используйте функцию отчета в боте
2. Введите фактические объемы
3. Проверьте обновление записи в Bitrix24

## 🔍 Мониторинг в реальном времени:

### Проверка записей в Bitrix24:
```bash
python tools/monitor_bitrix.py
```

### Проверка статуса бота:
```bash
python tools/check_bot_status.py
```

### Проверка логов бота:
```bash
Get-Content bot.log -Tail 20
```

## 📊 Ожидаемые результаты:

1. **При создании плана**:
   - В Bitrix24 создается объект с названием и адресом
   - Создается смена с плановыми объемами
   - Смена привязывается к объекту

2. **При завершении смены**:
   - Обновляются фактические объемы
   - Рассчитывается эффективность
   - Статус смены меняется на "закрыта"

## 🚨 Если что-то не работает:

1. Проверьте логи: `Get-Content bot.log -Tail 50`
2. Проверьте подключение к Bitrix24: `python tools/check_entities.py`
3. Перезапустите бота: `python app/telegram/bot.py`

## 📝 Команды для быстрой проверки:

```bash
# Проверка статуса системы
python tools/check_bot_status.py
python tools/monitor_bitrix.py

# Проверка подключения к Bitrix24
python tools/check_entities.py

# Просмотр логов
Get-Content bot.log -Tail 20
```

---
**Готово к тестированию!** 🎉

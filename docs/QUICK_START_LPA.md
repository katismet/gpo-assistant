# Быстрый старт тестирования ЛПА

## ✅ Можно сразу тестировать!

После запуска бота вы можете **сразу** протестировать ЛПА, если в системе есть закрытые смены.

## 🚀 Как протестировать

1. **Запустите бота** (если еще не запущен)

2. **Откройте бота в Telegram**

3. **Нажмите кнопку "🧾 ЛПА"** (или отправьте команду `/act:lpa`)

4. **Выберите объект** из списка

5. **Бот автоматически найдет закрытые смены:**
   - Сначала ищет в Bitrix24 (за последние 60 дней)
   - Если не находит, ищет в локальной БД
   - Показывает найденную смену с кнопками

6. **Нажмите "📄 Сгенерировать ЛПА"** или "❌ Отмена"

## 📊 Что будет в логах

Все действия записываются в логи с меткой `[LPA]`:

- `[LPA] User X selected object Y` - выбор объекта
- `[LPA] ✅ Found closed shift` - найдена закрытая смена
- `[LPA] No closed shifts found` - смены не найдены
- `[LPA] User X clicked 'generate_lpa' button` - нажата кнопка генерации

## ⚠️ Если смены не найдены

Если бот говорит "Закрытых смен не найдено", это значит:

1. **Нет закрытых смен в Bitrix24** для выбранного объекта
2. **Нет закрытых смен в локальной БД** для выбранного объекта

**Что делать:**
- Создайте план для объекта
- Создайте отчет с фактическими данными
- Смена закроется автоматически
- После этого можно будет сгенерировать ЛПА

## 🔍 Проверка логов после тестирования

```powershell
# Все логи ЛПА
Select-String -Path "logs\app.log", "bot.log" -Pattern "\[LPA\]"

# Последние 50 строк с ЛПА
Get-Content logs\app.log -Tail 100 | Select-String "\[LPA\]"
```

## ✅ Готово к тестированию!

Просто запустите бота и нажмите "🧾 ЛПА" - все логи будут записываться автоматически!



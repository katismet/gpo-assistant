# Диагностика: Бот молчит

## 🔍 Анализ логов

### Что найдено:
1. ✅ **Бот работал ранее** - последнее обновление обработано в 16:46:18
2. ⚠️ **Нет записей о `/start`** - команда не обрабатывается
3. ⚠️ **Есть записи "Update is not handled"** - некоторые обновления не обрабатываются
4. ⚠️ **Есть ошибки с фото** (не критично для /start)

## 🚨 Проблема

**Бот либо не запущен сейчас, либо не получает/не обрабатывает команду `/start`**

## ✅ Решение

### Шаг 1: Проверьте, запущен ли бот

```powershell
Get-Process python
```

Если процесса нет - **запустите бота**:
```powershell
python app/telegram/bot.py
```

### Шаг 2: После запуска отправьте `/start`

### Шаг 3: Проверьте логи в реальном времени

В другом окне терминала:
```powershell
Get-Content bot.log -Wait -Tail 20
```

Или проверьте логи после отправки:
```powershell
Get-Content bot.log -Tail 50 | Select-String "\[START\]|start|Update id"
```

## 📝 Что должно быть в логах

После отправки `/start` должны появиться записи:
```
[START] Command /start received from user X
[START] User X role: FOREMAN
[START] Keyboard created for user X
[START] Start command completed successfully
```

## ⚠️ Если бот запущен, но не отвечает

1. Проверьте логи на ошибки:
   ```powershell
   Get-Content bot.log -Tail 100 | Select-String "error|Error|exception|Exception"
   ```

2. Проверьте, получает ли бот обновления:
   ```powershell
   Get-Content bot.log -Tail 20 | Select-String "Update id"
   ```

3. Перезапустите бота (Ctrl+C и снова запустите)

## 🔧 Быстрая проверка

1. **Бот запущен?** → `Get-Process python`
2. **Есть новые обновления?** → Проверьте последние записи в `bot.log`
3. **Обрабатывается /start?** → Ищите `[START]` в логах

**Главное: убедитесь, что бот запущен и работает!**



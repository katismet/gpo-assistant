# Статус бота

## ✅ Бот запущен в фоновом режиме

Бот был запущен командой:
```powershell
python app/telegram/bot.py
```

## 📝 Проверка статуса:

1. **Проверьте процессы Python:**
   ```powershell
   Get-Process python
   ```

2. **Проверьте последние логи:**
   ```powershell
   Get-Content bot.log -Tail 30
   ```

3. **Проверьте логи запуска:**
   ```powershell
   Get-Content bot.log | Select-String "Запуск|Bot ID|polling"
   ```

4. **Отправьте `/start` в боте** и проверьте логи:
   ```powershell
   Get-Content bot.log -Tail 50 | Select-String "\[START\]"
   ```

## ⚠️ Если бот не отвечает

Если бот не отвечает на команды:
1. Проверьте, что процесс Python запущен
2. Проверьте логи на ошибки
3. Перезапустите бота

## 🔍 Мониторинг в реальном времени

Для мониторинга логов в реальном времени:
```powershell
Get-Content bot.log -Wait -Tail 20
```


